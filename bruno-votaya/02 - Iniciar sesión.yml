info:
  name: 02 - Iniciar sesión
  type: http
  seq: 2

http:
  method: POST
  url: https://votaya.com.mx/api/auth/login
  body:
    type: json
    data: |-
      {
        "correo": "usuario.bruno02@gmail.com",
        "contrasena": "Password123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
